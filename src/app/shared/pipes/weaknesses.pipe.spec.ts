import { WeaknessesPipe } from './weaknesses.pipe';

describe('WeaknessesPipe', () => {
  it('create an instance', () => {
    const pipe = new WeaknessesPipe();
    expect(pipe).toBeTruthy();
  });
});
