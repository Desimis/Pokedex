import { FlavourTextPipe } from './flavour-text.pipe';

describe('FlavourTextPipe', () => {
  it('create an instance', () => {
    const pipe = new FlavourTextPipe();
    expect(pipe).toBeTruthy();
  });
});
